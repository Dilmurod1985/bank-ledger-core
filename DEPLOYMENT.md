# 🚀 Деплой Bank Ledger Core

## 📋 Обзор

Этот документ описывает процесс развертывания банковской системы на облачных платформах.

## 🔧 Подготовка

### 1. Переменные окружения

Приложение поддерживает следующие переменные окружения:

#### База данных:
- `DATABASE_URL` - полный URL базы данных PostgreSQL (для продакшена)
- `DB_DRIVER` - тип базы данных (`postgres` или `sqlite`)
- `DB_HOST` - хост базы данных (для разработки)
- `DB_PORT` - порт базы данных (по умолчанию `5432`)
- `DB_USER` - пользователь базы данных
- `DB_PASSWORD` - пароль базы данных
- `DB_NAME` - имя базы данных
- `DB_SSLMODE` - режим SSL (по умолчанию `disable`)
- `DB_PATH` - путь к файлу SQLite (для локальной разработки)

#### Приложение:
- `PORT` - порт приложения (по умолчанию `8080`)
- `GIN_MODE` - режим Gin (`debug` или `release`)

## 🐳 Docker

### Локальный запуск с Docker:

```bash
# С PostgreSQL
docker-compose up -d

# Только приложение (с SQLite)
docker build -t bank-ledger-api .
docker run -p 8080:8080 -e DB_DRIVER=sqlite bank-ledger-api
```

## ☁️ Render.com

### 1. Подготовка репозитория

1. Загрузите код на GitHub
2. Убедитесь, что `render.yaml` находится в корне репозитория

### 2. Настройка на Render

1. **Создайте аккаунт** на [render.com](https://render.com)
2. **Подключите GitHub** репозиторий
3. **Создайте Web Service:**
   - Name: `bank-ledger-api`
   - Environment: `Docker`
   - Branch: `main`
   - Root Directory: `/`
   - Docker Context: `./`
   - Dockerfile Path: `./Dockerfile`

4. **Создайте PostgreSQL:**
   - Name: `bank-ledger-db`
   - Database Name: `bank_ledger`
   - User: `postgres`

5. **Настройте переменные окружения:**
   ```
   DB_DRIVER=postgres
   DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@bank-ledger-db:5432/bank_ledger
   PORT=8080
   ```

6. **Добавьте Health Check:**
   - Path: `/health`
   - Interval: `30s`
   - Timeout: `5s`

### 3. Автоматический деплой

Render автоматически развернет приложение при каждом пуше в ветку `main`.

## 🚂 Railway

### 1. Подготовка

1. Установите [Railway CLI](https://docs.railway.app/getting-started/installation)
2. Войдите в аккаунт: `railway login`

### 2. Деплой

```bash
# Клонируйте репозиторий
git clone https://github.com/your-username/bank-ledger-core.git
cd bank-ledger-core

# Инициализируйте проект
railway init

# Добавьте PostgreSQL
railway add postgresql

# Задайте переменные окружения
railway variables set DB_DRIVER=postgres
railway variables set PORT=8080

# Деплой
railway up
```

### 3. Через веб-интерфейс

1. **Зайдите в Railway Dashboard**
2. **Нажмите "New Project"**
3. **Выберите "Deploy from GitHub repo"**
4. **Выберите репозиторий**
5. **Добавьте PostgreSQL сервис**
6. **Настройте переменные окружения**

## 🗄️ Настройка PostgreSQL

### Render PostgreSQL

1. **Создайте базу данных** в Render Dashboard
2. **Получите connection string** из настроек
3. **Добавьте в переменные окружения:**
   ```
   DATABASE_URL=postgresql://username:password@host:port/database
   ```

### Railway PostgreSQL

1. **Добавьте PostgreSQL** в проект
2. **Railway автоматически создаст** переменную `${{services.postgres.DATABASE_URL}}`
3. **Используйте ее** в конфигурации

### Внешний PostgreSQL

Для внешних провайдеров (Supabase, ElephantSQL, etc.):

1. **Получите connection string**
2. **Добавьте в переменные окружения:**
   ```
   DATABASE_URL=postgresql://username:password@host:port/database?sslmode=require
   DB_DRIVER=postgres
   ```

## 🔒 Безопасность

### 1. Пароли и ключи

- **Никогда не храните** пароли в коде
- **Используйте переменные окружения** для секретов
- **Регулярно меняйте** пароли базы данных

### 2. SSL/TLS

- **В продакшене всегда используйте** `sslmode=require`
- **Настройте HTTPS** на уровне платформы

### 3. Бэкапы

- **Настройте автоматические бэкапы** базы данных
- **Тестируйте восстановление** из бэкапов

## 📊 Мониторинг

### Health Checks

Приложение имеет эндпоинт `/health` для проверки состояния:

```bash
curl https://your-app.onrender.com/health
```

Ответ:
```json
{
  "status": "ok"
}
```

### Логи

- **Render:** Dashboard → Logs
- **Railway:** Dashboard → Logs
- **Локально:** `docker-compose logs -f`

## 🚨 Траблшутинг

### 1. Проблемы с базой данных

```bash
# Проверьте connection string
echo $DATABASE_URL

# Проверьте доступность
psql $DATABASE_URL -c "SELECT 1;"
```

### 2. Проблемы с портом

```bash
# Убедитесь что порт 8080 доступен
netstat -tlnp | grep 8080
```

### 3. Проблемы с SSL

```bash
# Для PostgreSQL с SSL
DATABASE_URL="postgresql://user:pass@host:5432/db?sslmode=require"
```

## 🔄 Обновление

### Render
- **Автоматически** при пуше в `main`
- **Вручную** через Dashboard

### Railway
- **Автоматически** при пуше
- **Вручную** через `railway up`

## 📞 Поддержка

- **Render:** [support@render.com](mailto:support@render.com)
- **Railway:** [Discord](https://discord.gg/railway)
- **GitHub Issues:** [Create Issue](https://github.com/your-username/bank-ledger-core/issues)

---

**Готово к продакшену!** 🎉
