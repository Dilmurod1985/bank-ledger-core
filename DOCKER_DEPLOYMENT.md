# 🐳 Docker Деплой - Исправленная версия

## 📋 Структура проекта

```
bank-ledger-core/
├── main.go                 # Главный файл приложения
├── go.mod                   # Go модули
├── go.sum                   # Go зависимости
├── Dockerfile               # Docker конфигурация (ИСПРАВЛЕНА)
├── docker-compose.yml       # Локальный запуск
├── render.yaml              # Render конфигурация (ИСПРАВЛЕНА)
├── railway.toml             # Railway конфигурация (ИСПРАВЛЕНА)
├── config/                 # Конфигурация БД
├── handlers/               # HTTP обработчики
├── middleware/             # Middleware для аутентификации
├── models/                 # Модели данных
├── routes/                 # Роуты API
├── services/               # Бизнес-логика
└── static/                 # Фронтенд
    ├── index.html
    └── login.html
```

## 🔧 Исправления в Dockerfile

### Проблема
- Ошибка миграции SQLite: `Cannot add a NOT NULL column with default value NULL`
- Конфликты при повторном деплое с существующей БД

### Решение
1. **Скрипт входа (entrypoint.sh)**:
   - Автоматическое удаление старой БД SQLite
   - Установка правильного пути `DB_PATH`
   - Запуск приложения

2. **Правильная последовательность миграции**:
   - Сначала `Session` (без зависимостей)
   - Затем `Account`, `Product`, `Order`, `Transfer`

3. **Переменные окружения**:
   - `DB_PATH` для SQLite
   - `GIN_MODE=release` для продакшена

## 🚀 Деплой на Render

### 1. Подготовка
```bash
# Убедитесь что структура плоская
ls -la
# Должны видеть main.go в корне, не вложенные папки
```

### 2. Конфигурация Render
```yaml
# render.yaml (ИСПРАВЛЕНА)
services:
  - type: web
    name: bank-ledger-api
    envVars:
      - key: DB_DRIVER
        value: postgres
      - key: DATABASE_URL
        fromDatabase:
          name: bank-ledger-db
          property: connectionString
      - key: GIN_MODE
        value: release  # ВАЖНО: продакшен режим
    startCommand: /app/entrypoint.sh  # ВАЖНО: наш скрипт входа
```

### 3. Шаги деплоя
1. **Загрузить код на GitHub**
2. **Создать Web Service на Render**
3. **Создать PostgreSQL**
4. **Настроить переменные окружения**

### 4. Автоматический деплой
Render автоматически:
- Соберет Docker образ
- Запустит entrypoint.sh
- Очистит старую БД (если SQLite)
- Создаст новую БД с правильной схемой

## 🚂 Деплой на Railway

### 1. Конфигурация Railway
```toml
# railway.toml (ИСПРАВЛЕНА)
[deploy]
startCommand = "/app/entrypoint.sh"  # ВАЖНО: наш скрипт входа

[services.variables]
GIN_MODE = "release"  # ВАЖНО: продакшен режим
```

### 2. Деплой через CLI
```bash
railway login
railway init
railway add postgresql
railway up
```

## 🔒 Логика предотвращения конфликтов

### Entry Point Script
```bash
#!/bin/sh
set -e

# Удаление существующей БД для предотвращения конфликтов
if [ -f "/app/data/bank_ledger.db" ]; then
    echo "Removing existing database to prevent migration conflicts..."
    rm -f /app/data/bank_ledger.db
fi

# Установка пути к БД
export DB_PATH="/app/data/bank_ledger.db"

# Запуск приложения
exec ./main
```

### Порядок миграции
```go
// ВАЖНО: Session первой (без внешних ключей)
err = db.AutoMigrate(
    &models.Session{},     // 1. Сессии
    &models.Account{},     // 2. Аккаунты
    &models.Product{},     // 3. Товары
    &models.Order{},       // 4. Заказы
    &models.Transfer{},   // 5. Переводы
)
```

## 🐛 Траблшутинг

### 1. Ошибка миграции
```
Ошибка: Cannot add a NOT NULL column with default value NULL
Решение: Удалить файл БД или использовать entrypoint.sh
```

### 2. Docker не найден
```
Ошибка: docker: command not found
Решение: Использовать Docker Desktop или docker-compose
```

### 3. Переменные окружения
```bash
# Проверка переменных на Render
echo $DATABASE_URL
echo $DB_DRIVER
echo $GIN_MODE

# Локально
export DB_DRIVER=sqlite
export GIN_MODE=debug
```

## 📊 Мониторинг

### Health Check
```bash
curl https://your-app.onrender.com/health
# Ответ: {"status":"ok"}
```

### Логи
```bash
# Render
# Dashboard → Logs → bank-ledger-api

# Railway
# Dashboard → Logs → bank-ledger-api
```

## ✅ Проверка перед деплоем

### 1. Локальная сборка
```bash
go build -o main .
./main
```

### 2. Docker сборка (если доступен)
```bash
docker build -t bank-ledger-api .
```

### 3. Структура проекта
```bash
# Должна быть плоской
find . -name "*.go" -o -name "Dockerfile"
# Ожидаемый результат: ./main.go, ./Dockerfile
```

## 🎯 Ключевые моменты

1. **Плоская структура** - нет вложенных папок с проектом
2. **Entry point script** - автоматическая очистка БД
3. **Правильный порядок миграции** - зависимости сначала
4. **Продакшен режим** - GIN_MODE=release
5. **Переменные окружения** - DATABASE_URL для PostgreSQL

## 🚀 Готово к продакшену!

Теперь проект должен успешно деплоиться на:
- ✅ Render.com
- ✅ Railway.app  
- ✅ Любая другая Docker-платформа

**Структура исправлена, миграции работают, деплой должен пройти успешно!** 🎉
